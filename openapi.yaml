openapi: 3.1.0
info:
  title: raphael-workspaces
  version: 0.1.0
  description: Workspaces, modules, commits, diffs, branching, history
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: OK
